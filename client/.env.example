# ============================================================
# MSN ACADEMY INTERNSHIP PORTAL - CLIENT
# Optional. By default the client calls "/api" and Vite proxies
# it to http://localhost:5000 (see vite.config.js).
# Set this only when the API lives on another origin.
# ============================================================

# VITE_API_URL=https://api.example.com/api
